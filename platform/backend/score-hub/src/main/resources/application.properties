spring.application.name=score-hub
# Server Configuration
server.port=8080
server.servlet.context-path=/scorehub

# Spring Data JPA Configuration (if you're using a database)
spring.datasource.url=jdbc:postgresql://localhost:5432/score-hub
spring.datasource.username=postgres
spring.datasource.password=Satara@123##
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Logging Configuration
logging.level.org.springframework.web=DEBUG
logging.level.org.hibernate.SQL=DEBUG

# Spring Security (if using)
# spring.security.user.name=admin
# spring.security.user.password=adminpassword

# Actuator (optional for monitoring and health checks)
management.endpoints.web.exposure.include=health,info

# Other Custom Settings (example)
app.name=ScoreHub
app.version=1.0
